package com.pranay.interview.greedy;

public class MaximumValueAtAGivenIndex {
    public static void main(String[] args) {
        int n = 4, index = 2,  maxSum = 6;
        int val = maxValue(n, index, maxSum);
        System.out.println("Maximum value : " + val);
    }

    private static int maxValue(int n, int index, int maxSum) {
        
        return 0;
    }
}
